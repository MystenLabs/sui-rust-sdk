//! Support library for examples if/when we add shared helpers.
//! Currently empty; examples are standalone in /examples/*.rs (Cargo example targets).

// Placeholder to satisfy the crate.
pub fn _placeholder() {}
